{% extends '::base.html.twig' %}
{% block menu -%}
{% embed  '::parametres/menu.html.twig' %}
{% block menu_maha_open %}open{% endblock %}
{% block menu_controle_open %}open{% endblock %}
{% block menu_controle_actif %}active{% endblock %}
{% endembed %}
{% endblock %}
{% block ariane -%}
    <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="{{ path('homepage') }}">Accueil</a></li>
          <li class="breadcrumb-item"><a href="{{ path('parametre_index') }}">Paramètres</a></li>
          <li class="breadcrumb-item"><a href="{{ path('admin_parametres_controle_index') }}">Points de contrôle</a></li>
          <li class="breadcrumb-item">Détail</li>
    </ol>
{% endblock %}
{% block pagetitle -%}VOIR UN POINT DE CONTROLE{% endblock %}
{% block actions %}
    <label class="btn btn-outline-secondary"><a href="{{ path('admin_parametres_controle_index') }}">LISTER</a></label>
    <label class="btn btn-outline-secondary"><a href="{{ path('admin_parametres_controle_edit', { 'id': controle.id }) }}">MODIFIER</a></label>
    <label class="btn btn-outline-secondary"><a href="{{ path('admin_parametres_controle_new') }}">CREER</a></label>
    <label class="btn btn-outline-secondary">
        {{ form_start(delete_form) }}
            <input type="submit" value="SUPPRIMER" onclick="return confirm('Etes-vous sûr de vouloir supprimer?');">
        {{ form_end(delete_form) }}
    </label>
{% endblock %}
{% block body %}
    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Libellé</th>
                <td>{{ controle.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ controle.code }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ controle.type }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ controle.detail }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{% if controle.actif %}Oui{% else %}Non{% endif %}</td>
            </tr>
            <tr>
                <th>Créé par</th>
                <td>{{ controle.creePar }}</td>
            </tr>
            <tr>
                <th>Modifié par</th>
                <td>{{ controle.modifierPar }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{% if controle.dateCreation %}{{ controle.dateCreation|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Date de dernière modification</th>
                <td>{% if controle.dateModification %}{{ controle.dateModification|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
