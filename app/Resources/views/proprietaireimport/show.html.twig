{% extends 'base.html.twig' %}

{% block body %}
    <h1>Proprietaireimport</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proprietaireImport.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ proprietaireImport.path }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_parametres_proprietaire_importer_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_parametres_proprietaire_importer_edit', { 'id': proprietaireImport.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
