   
<div class="table-responsive">
    Confirmez-vous l'ouverture du centre?
        <table id="liste_clients" cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered datatable dataTable no-footer">
            <thead>
                <tr>
                    
                    <th colspan="5" align="center">Cartes vierges : {{ centre.carteVierge }}</th>
                </tr>
            <tr>
                <th>CAISSE</th>
                <th>SOLDE INITIAL</th>
                <th>AIGUILLAGE SUR CAISSE</th>
                <th>CAISSIER</th>
                <th>PISTE</th>
                <th>CONTROLLEUR</th>
            </tr>
            </thead>
            <tbody>
            {% for chaine in chaines %}
                <tr>
                    <td>{{ chaine.caisse.numero }}</td>
                    <td>{{ chaine.caisse.soldeInitial|number_format(0, ',', ' ') }}</td>
                    <td>{% if chaine.caisse.aiguillage %}OUI{% else %}NON{% endif %}</td>
                    <td>{% if chaine.caisse.getAffectationActive %}
                            {{ chaine.caisse.getAffectationActive.agent.username }}
                        {% endif %}
                    </td>
                    <td>{{ chaine.piste.numero }}</td>
                    <td>
                    {% if chaine.piste.affectationsActives is not empty %}
                        {% for affectation in chaine.piste.affectationsActives %}
                            {{ affectation.agent.username }}<br/>
                        {%endfor%}
                    {% else %}
                        Pas d'agent affecté{%endif%}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

