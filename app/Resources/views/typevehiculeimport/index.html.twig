{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typevehiculeimports list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Path</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typeVehiculeImport in typeVehiculeImports %}
            <tr>
                <td><a href="{{ path('admin_parametres_typevehicule_importer_show', { 'id': typeVehiculeImport.id }) }}">{{ typeVehiculeImport.id }}</a></td>
                <td>{{ typeVehiculeImport.path }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_parametres_typevehicule_importer_show', { 'id': typeVehiculeImport.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_parametres_typevehicule_importer_edit', { 'id': typeVehiculeImport.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_parametres_typevehicule_importer_new') }}">Create a new typeVehiculeImport</a>
        </li>
    </ul>
{% endblock %}
