{% extends '::base.html.twig' %}
{% block menu -%}
{% include '::visite/menu.html.twig' %}
{% endblock %}
{% block menu_controle_actif %}active{% endblock %}
{% block ariane -%}
    <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="{{ path('homepage') }}">Accueil</a></li>
          <li class="breadcrumb-item"><a href="{{ path('visite_controle') }}">CONTROLES</a></li>
          <li class="breadcrumb-item">Controler</li>
    </ol>
{% endblock %}
{% block pagetitle -%}CONTROLER UN VEHICULE{% endblock %}
{% block actions %}
    <label class="btn btn-outline-secondary"><a href="{{ path('visite_controle') }}">Liste des controles</a></label>
{% endblock %}
{% block body %}
     <form method='POST' action='{{ path('visite_controleur') }}?id={{ visite.id }}'>
        <table class="table table-hover">
            <tbody>
                <tr>
                    <th>Véhicule</th>
                    <td>{{ visite.vehicule.immatriculation }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{% if visite.revisite %}REVISITE{%else%}NORMALE{%endif%}</td>
                </tr>
                {% for controle in controles %}
                    <tr>
                        <th>{{ controle.libelle }}</th>
                        <td>
                            <select id='{{ controle.code }}' name='{{ controle.code }}' required >
                                <option value="">Choisir</option>
                                {% for code in controle.codeMahaResultats %}<option value='{{ code.code }}'>{{ code.libelle }}</option>{% endfor %}
                            </select>
                        </td>
                    </tr>
                {% endfor %}
                <tr>
                    <td colspan ='2'><input type='submit' name='controleur' /></td>
                </tr>
            </tbody>
        </table>
    </form>
{% endblock %}
